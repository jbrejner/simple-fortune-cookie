docker network create simple_cookie
docker run --name backend  --rm --network simple_cookie -d simplecookie/backend
docker run --name frontend --rm --network simple_cookie -d -p 8080:8080 --env BACKEND_DNS=backend simplecookie/frontend 
